import React from 'react';

const HeaderSection = (props) => (
    <div className="header-section text-center">
        <h1>Ling App</h1>
        <p className="lead">Convert DFA, NFa, Regex, Grammars to each other.</p>
    </div>

);

export default HeaderSection;